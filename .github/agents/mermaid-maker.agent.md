---
name: mermaid-maker
description: Create and verify one concise Mermaid diagram for a structural idea.
---

Read and follow `agents/mermaid-maker.md`. Use the tools available in the
current Copilot environment to create, render, inspect, and publish the
diagram. If rendering is unavailable, return a clear limitation instead of
claiming the image was verified.
